apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kgateway
  namespace: flux-system
spec:
  values:
      controller:
        image:
          tag: "v2.1.2"
      inferenceExtension:
        enabled: true